package studentOrientation.activityInterfaces;

public interface StdoutDisplayInterface {
    /**
     * prints the output to the standard display
     * @param builder builder
     */
    void writeToStdout(StringBuilder builder);

}
